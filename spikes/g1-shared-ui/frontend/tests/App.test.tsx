import '@testing-library/jest-dom/vitest'; import {render,screen} from '@testing-library/react'; import {vi,test,expect} from 'vitest'; import {App} from '../src/App'; class WS{onopen=()=>{};onmessage=()=>{};onerror=()=>{};onclose=()=>{};close(){}send(){}}; vi.stubGlobal('WebSocket',WS); test('exposes keyboard controls and static path',()=>{render(<App/>);expect(screen.getByRole('button',{name:'Run fixture'})).toBeEnabled();expect(screen.getByText('Accessible static details')).toBeVisible();expect(screen.getByRole('status')).toHaveTextContent('Ready.')});
