import { useEffect, useRef, useState } from 'react';
const BASE='http://127.0.0.1:43116/v1'; const TOKEN='g1-local-fixture';
type Phase='ready'|'loading'|'success'|'cancelled'|'disconnected'|'error';
export function App(){const [phase,setPhase]=useState<Phase>('ready'); const [message,setMessage]=useState('Ready.'); const abort=useRef<AbortController|null>(null); const socket=useRef<WebSocket|null>(null);
 const connect=()=>{socket.current?.close(); const ws=new WebSocket(`ws://127.0.0.1:43116/v1/events?token=${TOKEN}&version=1`); socket.current=ws; ws.onopen=()=>{setPhase('ready');setMessage('Event connection ready.')}; ws.onmessage=e=>setMessage(String(e.data)); ws.onerror=()=>{setPhase('error');setMessage('Event connection error. Use reconnect.')}; ws.onclose=()=>setPhase(p=>p==='error'?p:'disconnected')}; useEffect(()=>{connect();return()=>socket.current?.close()},[]);
 const run=async()=>{abort.current=new AbortController();setPhase('loading');setMessage('Loading deterministic fixture…');try{const r=await fetch(`${BASE}/fixture`,{headers:{Authorization:`Bearer ${TOKEN}`,'Nexa-Protocol-Version':'1','Content-Type':'application/json'},signal:abort.current.signal});const body=await r.json() as {message?:string,error?:string};if(!r.ok)throw new Error(body.error??'Request rejected');setPhase('success');setMessage(body.message??'Fixture complete.')}catch(e){if(e instanceof DOMException&&e.name==='AbortError'){setPhase('cancelled');setMessage('Request cancelled safely.')}else{setPhase('error');setMessage(`Recoverable error: ${e instanceof Error?e.message:'unknown'}`)}}};
 const cancel=()=>{abort.current?.abort();socket.current?.send(JSON.stringify({type:'cancel',request_id:'fixture'}))};
 return <main><a className="skip" href="#controls">Skip to controls</a><header><p className="eyebrow">Disposable evidence · Issue #116</p><h1>Nexa shared client fixture</h1><p>One static text path is used in browser and desktop. Animation is never required.</p></header><section id="controls" aria-labelledby="fixture-title"><h2 id="fixture-title">Lifecycle fixture</h2><div className="controls"><button onClick={run}>Run fixture</button><button onClick={cancel} disabled={phase!=='loading'}>Cancel</button><button onClick={connect}>Reconnect</button></div><div className={`status ${phase}`} role={phase==='error'?'alert':'status'} aria-live="polite"><strong>{phase}</strong><span>{message}</span></div></section><details><summary>Accessible static details</summary><p>The fixture reports loading, success, cancellation, disconnect, and recoverable error states as text.</p></details></main>}
